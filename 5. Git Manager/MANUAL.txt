Git Manager 260819_2 간단 매뉴얼
================================

[PC/바탕화면이 지워진 뒤 표준 복구]
1. BoardRepo/ChatGPT 등에서 최신 Automation_Manager_YYMMDD_N.zip 하나를 받습니다.
2. 원하는 위치에 압축을 풉니다. 폴더 이름은 Release마다 달라도 괜찮습니다.
3. 그 폴더 안의 Automation_Manager.pyw를 실행합니다.
4. Git Manager 탭에서 [현재 폴더 Git 연결/복구]를 누릅니다.
5. program_catalog.json의 기본 origin/branch가 자동 사용됩니다. GitHub URL을 다시 입력할 필요가 없습니다.
6. 확인창에 표시된 자동 origin/main을 확인하고 진행합니다.
7. 현재 폴더 자체에 .git이 생성되고 기존 origin/main 이력이 연결됩니다.
8. 현재 로컬 파일은 덮어쓰지 않습니다.
9. 원격에만 있는 1~4 관리자료는 복원하고, 5/6은 Root Release/문서만 보완합니다.
10. 자동 Push는 하지 않습니다. 상태를 확인한 뒤 필요한 경우 Commit + Push합니다.

[260819_2 수정]
- Git HEAD/origin의 legacy 경로 `6. boardrepo`와 현재 `6. BoardRepo`를 동일 target으로 처리합니다.
- Release와 Tree SHA가 같은데 `원격만 있음`으로 표시되던 case-only alias 오판을 수정했습니다.
- 상태 확인 과정에서 자동 case-only git mv/rename은 수행하지 않습니다.
- 실제 Git Tree에 서로 다른 alias 엔트리가 동시에 존재할 때만 `확인 필요`로 처리합니다.
- 표준 현재 폴더 복구에서는 Repository URL 입력창을 제거했습니다.

[Clone]
Clone은 원격을 기준으로 별도의 새 작업 폴더를 만들고 싶은 경우에만 사용합니다.
재난복구의 표준 절차에는 Clone이 필요하지 않습니다.

[보존 안전정책]
- main single branch
- Pull = git pull --ff-only
- no force push
- no automatic merge/rebase/conflict resolution
- reset --hard / clean 자동 사용 금지
- 과거 Automation Manager Release의 원격 덮어쓰기 Push 차단
